"""LLM-agent automation layer over meta_ads_pipeline.

The agent proposes; deterministic code executes. All Meta-side effects flow
through the existing plan -> apply seam, gated on human approval in the
workbook's BulkChanges sheet.
"""
